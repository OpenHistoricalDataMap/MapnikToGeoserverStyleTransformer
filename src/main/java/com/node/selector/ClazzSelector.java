package com.node.selector;

import com.node.NodeGroup;
import com.node.SelectorNode;

public class ClazzSelector  extends SelectorNode {
    public ClazzSelector(String raw, NodeGroup parent, String selector) {
        super(raw, parent, selector);
    }
}
