package com.node.selector;

import com.node.NodeGroup;
import com.node.SelectorNode;

public class StyleSelector extends SelectorNode {
    public StyleSelector(String raw, NodeGroup parent, String selector) {
        super(raw, parent, selector);
    }
}
