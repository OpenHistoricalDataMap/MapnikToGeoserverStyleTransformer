package com.node.selector;

import com.node.NodeGroup;
import com.node.SelectorNode;

public class ZoomSelector extends SelectorNode {
    public ZoomSelector(String raw, NodeGroup parent, String selector) {
        super(raw, parent, selector);
    }
}
