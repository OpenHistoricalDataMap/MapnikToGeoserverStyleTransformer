package com.node.selector;

import com.node.NodeGroup;
import com.node.SelectorNode;

public class SubclazzSelector extends SelectorNode {

    public SubclazzSelector(String raw, NodeGroup parent, String selector) {
        super(raw, parent, selector);
    }
}
